# Extract module-a's latest (= 1.2.3 per VERSION file) changelog entry
> extractLatestChangelog module-a

# Verify the body matches expected exactly (only the section under ## 1.2.3, trimmed)
$ must-mirror target/changeset/changelog.md expected/module-a.md

# Running for a different module overwrites with that module's body (its own latest)
> extractLatestChangelog module-b
$ must-mirror target/changeset/changelog.md expected/module-b.md

# Unknown module fails clearly
-> extractLatestChangelog module-does-not-exist
