# Initialize git repo and create initial commit with all files
$ exec git init
$ exec git config user.email "test@test.com"
$ exec git config user.name "Test"
$ exec git add .
$ exec git commit --no-gpg-sign -m "Initial commit"

# Run the command — no module sources changed, only the changeset file in HEAD
> changesetMatrix validate

# Verify the enriched matrix is emitted (sorted, with snapshot version + coordinate)
$ must-mirror target/changeset/matrix.json expected/matrix.json

# Unknown stage fails clearly
-> changesetMatrix nope
