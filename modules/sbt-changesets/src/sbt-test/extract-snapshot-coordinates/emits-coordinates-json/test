# Run the command for both modules
> extractSnapshotCoordinates module-a module-b

# Verify the JSON matches expected (sorted, with per-module org and snapshot version)
$ must-mirror target/changeset/snapshot-coordinates.json expected/snapshot-coordinates.json

# Unknown module fails clearly
-> extractSnapshotCoordinates does-not-exist

# No args fails clearly (Usage error)
-> extractSnapshotCoordinates
